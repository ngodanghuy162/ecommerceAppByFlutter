const incomeField = "income";

const orderField = "order";

const productField = "product";

const voucherField = "voucher";

const ownerField = "owner";

const addressField = "address";

const shopImageField = "shopImage";

const nameField = "name";
